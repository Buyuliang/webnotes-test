# week3

## Week 3（2D U-Net 的完整实现与训练）



### 学习目标：

用真实数据训练一个完整的 2D U-Net，理解前向传播、损失计算和评估。

### 作业/实验场景：


实现完整的 2D U-Net，包含 down-sampling 路径、上采样路径、跳跃连接。
使用 Dice 损失（可与 BCE 结合）训练，监控训练/验证 Dice。
记录训练曲线（损失、Dice、IoU），评估一个简单的基线模型。

### 输出物：

训练脚本、验证脚本、曲线图、模型的最初预测示例。