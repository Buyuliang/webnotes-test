# week2

## Week 2（数据准备与预处理）



### 学习目标：

掌握分割任务的数据准备、数据增强与标签对齐技巧。

## 作业/实验场景：


选择一个公开数据集（如 ISIC 或 DRIVE），下载并制作训练/验证集。
实现数据加载管道（Dataset + DataLoader），并实现常用数据增强：随机翻转、旋转、缩放、亮度/对比度调整、简单的几何变换。
进行简单的统计尺度归一化（如像素值归一化到 [0,1] 或 零均值单位方差）。

## 输出物：

数据加载/增强代码、一个可运行的训练循环的初始版本（不一定要收敛，但能跑通）。