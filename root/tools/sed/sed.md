# sed

## vim 中替换
```bash
:[范围]s/旧文本/新文本/[选项]
```
```bash
:%s#\\#^#g
```
<span style="font-size: 10px">**可更换分隔符**</span>

```bash
范围：指定替换的行

% → 全文件

. → 当前行

1,10 → 第 1 行到第 10 行

旧文本：你要替换的字符串

新文本：替换成的字符串

选项：

g → 行内替换所有匹配（默认只替换每行第一个）

c → 替换前询问确认
```






